# Redirect API calls to backend (for local development)
# Update the URL to your actual backend URL when deployed
/api/* https://ir-railvision-backend.onrender.com/api/:splat 200

# SPA fallback - serve index.html for all routes
/* /index.html 200
